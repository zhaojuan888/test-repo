# IMA 知识库 MCP → 审迹 RAG 消幻觉 接入方案

> 目的：回答 CAS / 审计 / FDD / 税务 / 行业 类问题时，先经 IMA 知识库检索（RAG）兜底、
> 引用来源、避免幻觉；把"凭记忆答"变成"检索+引用答"。
> 创建：2026-08-23（由自动化整理任务触发）

## 一、当前状态（重要）

- **本会话 `ima-mcp` 连接状态：disconnected**（见会话 connector-status）。
  - 历史记忆显示 2026-08-18 曾 connected；本会话掉线，需重新在 WorkBuddy 连接器设置中
    **点击连接 / 信任 `ima-mcp`** 后才能调用 `mcp__ima-mcp__search_knowledge`。
- IMA 侧已有 3 个知识库（ID 见下），均含公开/半公开审计资料，**可直接当 RAG 叠层**。

## 二、IMA 侧已有知识库（RAG 检索目标）

| 库 ID | 名称 | 内容 | 适用问题 |
|---|---|---|---|
| `7472842945880280` | 审计实务资料库（185MB/43条） | 普华审计实务指南（工业/金融/工程/舞弊/合规/风险导向内审/内控…）、主要事项核查指引、财务舞弊易发高发领域、IPO尽职调查核查页、年报审计资料清单、财务尽职调查资料清单、关键控制点、财务制度汇编 | **审计程序 / 内控 / IPO核查 / FDD 清单 / 舞弊** |
| `7472556269373096` | 行业审计参考（46MB/21条） | 行业研究类 | 行业审计风险 |
| `7472556453929268` | 政府会计相关（2MB/12条） | 政府会计类 | 政府会计 / 行政事业单位 |

## 三、⚠️ 敏感不出域红线（必须守住）

- 审迹库（`D:\AI学习\审迹知识库`）含**客户底稿 / 函证 / 未审报表**等敏感材料。
- **禁止**把 vault 中含 `sensitive` 标记的笔记或 1-项目/客户资料整体上传到云端 IMA。
- RAG 只走"**查询 IMA 既有公开资料库**"方向，**不反向把 vault 上传 IMA**。
  - 仅有公开属性、无客户信息的通用参考资料（如准则原文、公开行业指南）才可经
    `mcp__ima-mcp__add_knowledge` / `import_urls` 入库；且入库前确认不含敏感字段。

## 四、RAG 调用约定（连接恢复后执行）

回答以下任一类问题前，**先检索再作答**，并标注来源库：
- 会计准则条文 / 应用指南（CAS ×）
- 审计程序 / 内控 / 舞弊 / 截止测试 / 抽样
- IPO 核查 / FDD 资料清单 / 财务尽职调查
- 行业审计风险 / 政府会计

检索工具：`mcp__ima-mcp__search_knowledge`（参数仅 `knowledge_base_id` + `query`，无 limit 字段）。
列举库：`mcp__ima-mcp__get_knowledge_base_list`（type 须为有效值如 `KBT_MINE_KB`）。

**推荐检索顺序**：审计实务资料库（`7472842945880280`）→ 命中即用；行业类追加行业审计参考（`7472556269373096`）；政府会计追加（`7472556453929268`）。

**回答模板**：
> 据 IMA「审计实务资料库」（来源：<条目名>）…（引用要点）。
> 若 IMA 无相关条目，注明"未检索到权威来源，以下为一般性判断，需核实"。

## 五、用户侧接入步骤（需人工）

1. 打开 WorkBuddy → 设置 → 连接器 → 找到 `ima-mcp` → 点击**连接 / 信任**。
2. 授权后本会话即可调用 `mcp__ima-mcp__search_knowledge`。
3. （可选）把**公开**参考资料经 IMA 导入工具入库，扩充 RAG 覆盖；**勿传敏感**。
4. 验证：问一个 CAS 条文类问题，确认回答带"据 IMA 审计实务资料库"引用。

## 六、与本地 Karpathy 管线关系

- 本库方法论是"一次消化、持久积累"（编译为 wiki），**不靠每次 RAG 检索原始文档**。
- IMA RAG 是**补充兜底**：在 AI 作答 CAS/审计判断前做事实校验，消幻觉；
  检索到的权威条目应**消化**为 2-知识库 原子笔记（带 `[[wikilink]]`），而非长期依赖 RAG。
- 二者不冲突：RAG=事实护栏，wiki=主记忆。
